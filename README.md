# Lightning Browse

Final project for a learning course on [LightningJS](https://lightningjs.io/). Consists of fetching upcoming movies from [The Movie Database API](https://www.themoviedb.org/) and rendering them in both list and detail views.

#### Running the App

1. Install dependencies by running `yarn install`

2. Run the app in dev mode by running `yarn dev`
